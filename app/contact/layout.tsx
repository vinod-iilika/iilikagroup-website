export const metadata = {
  title: "Contact Us",
  description:
    "Get in touch with IILIKA GROUPS for IT staffing, GCC enablement, and project delivery inquiries. Reach out as a client or apply as a candidate.",
};

export default function ContactLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
